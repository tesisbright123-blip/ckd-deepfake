# MacBook (Apple Silicon M-series) requirements for edge evaluation.
#
# Install with:
#   uv pip install -r requirements-macbook.txt
# or:
#   pip install -r requirements-macbook.txt
#
# This is NOT a superset of requirements.txt — it's a lean spec for just
# the edge-eval workflow (no training, no soft-label generation). Install
# alongside an editable install of the repo:
#   uv pip install -e .

# --- Core ML stack (CPU-only on M2 Max is fine; no CUDA) ---
torch>=2.1,<2.5
torchvision>=0.16,<0.20

# --- Backbone library (auto-detects feature dim) ---
timm>=1.0

# --- Conversion pipeline ---
onnx>=1.15
onnxruntime>=1.17        # for ONNX-level sanity
onnx2tf>=1.22            # ONNX -> TensorFlow SavedModel -> TFLite

# TensorFlow on Apple Silicon: use the tensorflow-macos wheel.
# tensorflow-metal is optional — TFLite Python uses CPU XNNPACK anyway.
tensorflow-macos>=2.13,<2.16; sys_platform == 'darwin' and platform_machine == 'arm64'
tensorflow>=2.13,<2.16; sys_platform != 'darwin'

# Lightweight TFLite runtime — preferred over full tensorflow at inference time.
ai-edge-litert>=1.0; python_version >= '3.10'

# --- CoreML (mac-only) ---
coremltools>=7.0; sys_platform == 'darwin'

# --- Data + eval ---
numpy>=1.24,<2.0
pandas>=2.0
opencv-python-headless>=4.8
albumentations>=1.3,<2.0
scikit-learn>=1.3        # used in src/evaluation/metrics.py
pyyaml>=6.0

# --- Plotting (figures for thesis) ---
matplotlib>=3.7
seaborn>=0.13
